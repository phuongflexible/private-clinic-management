const Footer = () => {
    return (
        <footer className="bg-dark text-white text-center py-4">
            <div>&copy; {new Date().getFullYear()} Private Clinic Website</div>
        </footer>
    );
}

export default Footer;